type Level = 0 | 1 | 2 | 3 | 4;
type Contribution = {
    date: string;
    count: number;
    level: Level;
};
type ContributionResponse = {
    total: {
        [year: number]: number;
        [year: string]: number;
    };
    contributions: Array<Contribution>;
};
type NestedContributionResponse = {
    total: {
        [year: number]: number;
        [year: string]: number;
    };
    contributions: Record<number, Record<number, Record<number, Contribution>>>;
};
type Format = 'array' | 'nested';
interface GetContributionOptions {
    year?: 'all' | 'last' | number | number[];
    format?: Format;
    cache?: boolean;
}

declare function buildCacheKey(username: string, opts?: GetContributionOptions): string;
declare function getContribution(username: string, opts?: GetContributionOptions): Promise<ContributionResponse | NestedContributionResponse>;

export { type Contribution, type ContributionResponse, type Format, type GetContributionOptions, type Level, type NestedContributionResponse, buildCacheKey, getContribution };
